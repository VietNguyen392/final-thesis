import React from 'react';
import type { NextPage } from 'next';

import { useAuth } from 'hooks';
const Home: NextPage = () => {
  // const {user}=useAuth()

  return <div>Home</div>;
};

export default Home;
